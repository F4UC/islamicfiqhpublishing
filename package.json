{
  "name": "islamicfiqhpublishing-functions",
  "version": "0.0.0",
  "private": true,
  "description": "Server-side dependencies for Cloudflare Pages Functions only. The site itself is static (no build step); this file exists so Pages bundles the server deps: jose (Google id_token), @simplewebauthn/server (passkeys), stripe. Keep the Pages build command EMPTY and output dir = repo root.",
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "jose": "^5.10.0",
    "stripe": "^22.3.0"
  }
}
