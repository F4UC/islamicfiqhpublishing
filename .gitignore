.claude/worktrees/

# Wrangler — keep all configs out of repo (use Cloudflare Pages native deploy)
wrangler.jsonc
wrangler.toml
.wrangler/

# Large binary / video files — host externally (Cloudflare R2, YouTube, etc.)
*.mp4
*.mov
*.avi
*.webm
*.mkv
