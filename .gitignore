.claude/worktrees/

# Wrangler (Workers CLI) — not used for Pages; keep out of repo
wrangler.jsonc
wrangler.toml
.wrangler/

# Large binary / video files — host externally (Cloudflare R2, YouTube, etc.)
*.mp4
*.mov
*.avi
*.webm
*.mkv
