.claude/worktrees/

# Wrangler — block auto-generated jsonc and cache dir; allow committed toml
wrangler.jsonc
.wrangler/

# Large binary / video files — host externally (Cloudflare R2, YouTube, etc.)
*.mp4
*.mov
*.avi
*.webm
*.mkv
